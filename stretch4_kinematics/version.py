# Store the version here so:
# 1) we don't load heavy dependencies when querying the version
# 2) setuptools can extract it dynamically at build time

BASE_VERSION = "2026.09.15"

__version__ = BASE_VERSION

